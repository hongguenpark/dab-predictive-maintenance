resources:
  pipelines:
    pm_dlt_pipeline:
      name: "[${bundle.target}] PM - 센서 데이터 SDP 파이프라인"
      target: predictive_maintenance
      catalog: ebay_anomaly_detection_catalog
      libraries:
        - notebook:
            path: ./src/dlt_sensor_pipeline.py
      channel: PREVIEW
      serverless: true
      configuration:
        source_schema: "ebay_anomaly_detection_catalog.predictive_maintenance"
